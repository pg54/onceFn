import EventBinder from '../core/event/event-binder'

export default function (Glide, Components, Events) {
    const Binder = new EventBinder()
    const Images = {
        mount () {
            this.bind()
        },
        bind () {
            
        }
    }
}