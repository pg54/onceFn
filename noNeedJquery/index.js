// effects

// Fade In
$(el).fadeIn();
function fadeIn(el) {
    el.style.opacity = 0;
}
fadeIn(el)

// Hide
$(el).hide();
el.style.display = 'none';

// show
$(el).show();
el.style.display = '';

// ELEMENTS